[project]
name = "ziptrigo-shared-utils"
version = "0.1.0"
description = "Shared utilities for Ziptrigo services"
dependencies = [
    "django>=6.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
