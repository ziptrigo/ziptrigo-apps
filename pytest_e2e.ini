[pytest]
# E2E tests don't use django-pytest directly because they run against a separate process
# This prevents pytest-django from trying to find a django project at the root
DJANGO_SETTINGS_MODULE =
addopts = -v --tb=short
testpaths = tests_e2e
